import { Component, Inject } from 'ng-metadata/core';


@Component({
    selector: 'my-team-box',

})
export class MyTeamBoxComponent {
    constructor(
        // @Inject(TeamResourceService) private teamResourceService: TeamResourceService
    ) {

    }
}